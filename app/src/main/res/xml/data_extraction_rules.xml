<?xml version="1.0" encoding="utf-8"?>
<!--
  Lumi holds notes, documents, journal entries, routines, and emergency contacts.
  None of it is uploaded, so none of it goes into cloud backup or device transfer
  either. android:allowBackup is already false; this file makes the intent explicit
  for the transfer path too.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
